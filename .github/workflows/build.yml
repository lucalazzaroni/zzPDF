name: Build

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build release
        run: swift build -c release
      - name: Run smoke tests
        run: ./scripts/run-tests.sh
